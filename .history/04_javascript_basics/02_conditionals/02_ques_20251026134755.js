//? check if one string is eual to another string or not.
let string1 = "hello";
let string2 = "hello";  
if (string1 === string2) {
  console.log("Both strings are equal");
} else {
  console.log("Strings are not equal");
}
//? == may perform type coercion, while === checks for both value and type equality.
