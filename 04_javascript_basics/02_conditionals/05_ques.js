//? check if an array is empty or not.
let arr = [];   
if (arr.length === 0) { //&& arr.length checks the number of elements in the array
  console.log("The array is empty");
} else {
  console.log("The array is not empty");
}